# Legacy classifier workflow

These are the original eight notebooks from commit `94aabec`, preserved byte for byte.
Run them with the repository root as the working directory if reviewing the old run.
Their five-class workflow and validation requirements do not gate the new descriptive
forest-change notebooks in the parent directory.

Existing rasters, reference candidates, validation packages and outputs remain in
place. `outputs/forest_change/preserved_inputs.json` records their pre-refactor
SHA256 hashes. All new analysis outputs are under `outputs/forest_change/` and
`data/stacks/forest_change/`; no legacy dNBR interpretation was changed.
