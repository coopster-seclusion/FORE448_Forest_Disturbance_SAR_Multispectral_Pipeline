# Optional: use existing Earth Engine OAuth credentials; never commit this file filled in.
GEE_PROJECT=
# Optional Earthdata credentials, only needed for authenticated downloads / HyP3.
EARTHDATA_USERNAME=
EARTHDATA_PASSWORD=
